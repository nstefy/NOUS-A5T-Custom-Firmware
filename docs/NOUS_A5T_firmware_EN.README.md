# NOUS A5T Smart Power Strip - Custom Firmware (v2.7.3)

Professional-grade custom firmware for the **NOUS A5T Smart Power Strip** (based on the ESP8285 chip). This firmware replaces the stock software to provide enhanced privacy, local control via Web UI/MQTT, and highly accurate energy monitoring.

## Key Features

- **Multi-Socket Control**: Independent control for 3 standard AC outlets and 1 USB port.
- **Advanced Energy Monitoring**: High-precision monitoring of Voltage (V), Current (A), Active Power (W), and Power Factor (PF) via the CSE7766 chipset.
- **mDNS**: Easy access via unique hostname (e.g., `http://nous-a5t-XXXXXX.local` where XXXXXX is the Chip ID).
- **Industrial-Grade Reliability**: 
    - **RAM Optimized**: High-efficiency string management ensuring ~25KB free heap.
    - **WiFi Resilience**: Automatic background reconnection and emergency AP fallback (after 5 minutes of disconnect).
    - **Safety First**: Automatic cut-off at **3680W (16A)**.
- **Smart Home Ready**: 
    - Full MQTT support with Home Assistant Auto-Discovery.
    - Built-in configuration exporters for **OpenHAB** and **ESPHome**.
- **Power-on Behavior**: Configurable startup states: `ALWAYS OFF`, `ALWAYS ON`, or `RESTORE PREVIOUS`.
- **Physical Security**: "Child Lock" feature to disable physical button interactions.
- **Web Security**: Multi-tier Basic Authentication for the main dashboard and configuration menus.

## Web Interface

- **Main Dashboard**: Real-time toggle buttons and live sensor telemetry (2s auto-refresh).
- **MQTT/Network Config**: Comprehensive setup for broker details, mDNS hostname, and Discovery settings.
- **Security Center**: Manage access credentials, OTA passwords, and password masking preferences.
- **Calibration Suite**: Built-in wizard for automated or manual factor adjustment.

## MQTT Integration

The base topic defaults to `nous`. All command topics expect `ON`/`OFF`, `1`/`0`, or `TRUE`/`FALSE`.

### Control & State
| Feature | State Topic | Command Topic |
| :--- | :--- | :--- |
| **Relays 0-3** | `nous/relay/[0-3]` | `nous/relay/[0-3]/set` |
| **All Relays** | - | `nous/relay/all/set` |
| **Child Lock** | `nous/child_lock` | `nous/child_lock/set` |
| **Power-on Behavior** | `nous/power_on_behavior` | `nous/power_on_behavior/set` |

### Telemetry & Sensors
| Sensor | Topic | Unit | Description |
| :--- | :--- | :--- | :--- |
| **Voltage** | `nous/voltage` | V | Mains voltage |
| **Current** | `nous/current` | A | Current draw |
| **Power** | `nous/power` | W | Active power consumption |
| **Power Factor** | `nous/pf` | - | Efficiency ratio (0.00 - 1.00) |

### System Statistics
- `nous/stats/wifi`: Total WiFi reconnections since boot.
- `nous/stats/mqtt`: Total MQTT reconnections since boot.
- `nous/stats/reset`: Send any payload to reset counters.

## Installation & Setup

1. **First Boot**: If no WiFi is configured, the device hosts an AP: **"NOUS-Setup"**.
2. **Web Config**: Connect your phone/PC to the AP and go to `http://192.168.4.1`. 
3. **Network Scan**: Select your home network from the scanned list, enter the password, and save.
4. **Automatic Discovery**: In Home Assistant, ensure the MQTT integration is active. The power strip will appear automatically as a new device with all 4 switches and 4 sensors.

## Physical Buttons

### Main Button (Side)
- **Short Press**: Toggles all 4 sockets simultaneously.
- **Long Press (>10s)**: Initiates a **Factory Reset** (formats the file system and reboots).

### Individual Buttons (Top)
- Controlled via internal ADC.
- Buttons 1, 2, and 3 control Sockets 1, 2, and 3 respectively.

## Safety

- **Overload Protection**: If the measured power exceeds **3680W**, the firmware enters a "Fault" state, kills all relays immediately, and displays a warning on the Web UI.

## Developer Information

### Hardware Specs
- **Module**: ESP8285 (1MB Flash).
- **Energy Chip**: CSE7766 (UART @ 4800 baud, 8E1).
- **Storage**: LittleFS used for configuration persistence.
- **mDNS**: Reachable via `http://nous-a5t-XXXXXX.local` (mDNS).

### Performance Optimizations
- **String Buffering**: Heavy use of `reserve()` and `+=` operator to prevent heap fragmentation.
- **Flash Strings**: Static HTML/CSS stored in PROGMEM via `F()` macro to maximize available RAM.
- **Non-Blocking Logic**: All network and sensor handlers are ne-blocking to ensure physical button responsiveness.

## License & Versions

**Current Version**: `2.7.3-EN` (or `2.7.3-RO` for Romanian translation).

*This firmware is provided as-is. Always exercise caution when working with mains voltage.*

---
*Documentation generated by Gemini Code Assist.*